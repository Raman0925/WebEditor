export const publicRoutes: string[] = [];

export const protectedRoutes: string[] = [
  "/dashboard",
  "/settings",
  "/profile",
  "/admin",
  "/premium",
];

export const authRoutes: string[] = ["/admin"];

export const apiAuthPrefix: string = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT: string = "/";
